connect =  dict(
            server = "SERVER_IP"
            )
login =  dict(
            user = "SUPER_ADMIN_USER",
                password = "PASS",
                )
group = dict(
            name = "ZABBIX_PROXY_GROUP"
            )
log = dict(
        file = "/var/log/zabbix/proxy_balancer.log"
        )
